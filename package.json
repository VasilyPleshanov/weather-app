{
  "name": "weather-app",
  "private": true,
  "homepage": "https://vasilypleshanov.github.io/weather-app/",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "gh-pages -d dist",
    "predeploy": "npm run build"
  },
  "devDependencies": {
    "sass": "^1.62.0",
    "vite": "^4.2.0"
  },
  "dependencies": {
    "gh-pages": "^5.0.0",
    "normalize.css": "^8.0.1"
  }
}
